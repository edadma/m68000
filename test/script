/* OUTPUT_FORMAT(srec) */

SECTIONS {
    . = 0;
    .rodata : { *(.rodata) }
    .text : { *(.text) }

    . = 0x10000;
    .data : { *(.data) }
    .bss : { *(.bss) }

    /DISCARD/ : {
        *(.eh_frame);
        *(.comment);
    }
}